{
  "scripts": {
    "build": "yarn global add esbuild && esbuild --bundle plugin.ts > plugin.js --platform=node",
    "publish": "npm publish --access public"
  },
  "name": "@tulib/vite-tulip-plugin",
  "version": "0.0.0",
  "type": "module",
  "main": "plugin.js",
  "module": "plugin.js",
  "types": "plugin.ts",
  "private": false,
  "license": "MIT",
  "dependencies": {
    "picocolors": "1.0.1"
  },
  "peerDependencies": {
    "vite": "*"
  }
}
